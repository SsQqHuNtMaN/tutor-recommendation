# Viewer 固定视口与性能优化 TODO

状态：已完成
确认日期：2026-07-12

## P0：结构正确性与固定视口

### 日历位置和独立筛选

- [x] 把日历移动到顶部工具栏之后、教师列表筛选之前。
- [x] 在日历工具栏中增加独立的学校、学院筛选器。
- [x] 日历只受四周范围、日历学校和日历学院影响，不受教师搜索、推荐等级、套磁情况、“查看已套磁”、“隐藏已标记”和“低证据”等列表筛选影响。
- [x] 日历学院选项只显示当前日历学校下的学院。
- [x] 点击日历教师仍可更新详情；即使该教师不满足当前列表筛选，也不应阻止详情显示。

### 精简列表筛选

- [x] 删除最低分输入框和对应 JavaScript 筛选逻辑。
- [x] 调整筛选器网格，使剩余控件在常用宽度下不留异常空位。

### 页面不滚动

- [x] 桌面端 `body` 和应用根节点固定为视口高度，不产生页面级纵向滚动。
- [x] 顶栏、日历、教师筛选和工作区在一个视口内完成布局分配。
- [x] 教师表格和详情栏各自滚动；滚动表格不带动整个网页。
- [x] 日历在分配区域内管理自身滚动，不推动整个页面。
- [x] 1440×900 和 1366×768 下验证 `documentElement.scrollHeight <= innerHeight`。

## P1：首次加载与操作性能

### 性能基线

- [x] 在忽略的本地项目记忆中记录优化前 `/api/data` 首次响应时间、响应体大小、教师数和内嵌证据数；公开文档不保存当前本地结果计数。
- [x] 定位三类主要成本：首次读取多个 Excel sheet、传输和解析大 JSON、一次性创建全部表格行；前端首批渲染耗时未单独取得。

### 后端按需详情

- [x] `/api/data` 首屏只返回教师摘要和联系状态，不再为每位教师内嵌 DBLP、arXiv、网页和 WebSearch 明细。
- [x] 增加只读教师详情接口，按稳定记录 key 返回该教师的证据明细。
- [x] 服务端按工作簿签名缓存详情 sheet，避免重复选择同一工作簿教师时反复读取 Excel。
- [x] 工作簿变化后失效摘要和详情缓存；联系状态变化后失效组合响应缓存。
- [x] 保持现有 loopback、Host/Origin 和写接口安全边界不变。

### 前端渐进渲染

- [x] 教师表格首批只创建 120 行，滚动接近底部时每次追加 120 行。
- [x] 筛选变化时重置批次；表格始终保留完整筛选结果，只分批创建 DOM 行。
- [x] 搜索输入使用 160 ms 防抖，选择框和复选框仍即时响应。
- [x] 选择教师时只更新选中态和详情，不因列表选择重建日历。
- [x] 详情首次打开显示加载状态；请求完成后再渲染证据，重复打开使用前端缓存。

### 性能验收

- [x] 首屏表格 DOM 行数限制为 120 行，不再一次创建全部教师行。
- [x] 预热缓存后的 `/api/data` 响应时间和体积相对优化前明显下降，精确结果保存在忽略的本地项目记忆中。
- [x] 自动化搜索、筛选、选择教师和侧栏收展操作无明显长时间卡死。

优化后的精确计时和真实浏览器操作已通过本地 API 与无界面 Chrome 复测；公开文档不保存当前本地数据规模和精确运行计数。

## P2：凸出式侧栏把手

- [x] 删除详情内部“收起”和独立“展开教师详情”文字按钮。
- [x] 在详情栏左边缘增加向外凸出的单一把手。
- [x] 详情展开时把手显示 `>`，点击后向右收起；收起时显示 `<`，点击后从右侧展开。
- [x] 把手始终附着在列表与详情分界或工作区右边缘，不覆盖主要表格内容。
- [x] 保留 `aria-controls`、`aria-expanded`、键盘操作和折叠布尔偏好。
- [x] 中等宽度与移动端详情位于正常文档流时隐藏该把手。

## 总体验收

- [x] 日历和教师列表使用彼此独立的筛选状态。
- [x] 页面本身不滚动，表格、详情和必要时的日历内部滚动。
- [x] 日历回复颜色、范围导航和缺日期列表保留；后续四周条状布局记录见 `viewer-calendar-strip-todo.md`。
- [x] “查看已套磁”和“隐藏已标记”继续只影响教师列表。
- [x] 当前 43 项 Python 单元测试、JavaScript 语法检查、Python 编译检查和 `git diff --check` 通过。
- [x] 1440×900、1366×768 桌面和 390×844 移动端真实页面完成冒烟验证。
